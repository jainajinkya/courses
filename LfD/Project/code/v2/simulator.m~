function xNew = simulator(x,mu,u,mA,mB,mC,chpts)
global nState

idx = size(chpts,2)*ones(nState,1);

for i = 1:nState
    for j = size(chpts,2):-1:1
        if(x(i,1) < )
        
        end
    end
end

xNew = A*(x-mu) + (A*mu - B*u);

end
